import { IdFormatterPipe } from './id-formatter.pipe';

describe('IdFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new IdFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
